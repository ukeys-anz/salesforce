# **Permission Sets**


 - Use **Permission Sets** as much as possible (over **Profiles**) to grant user permissions.
 
 - We should try to maintain one for each functionality (i.e. `Customer Read/Only`, `IDR User`)
 
 - Changes to **Permission Sets** are easier to deploy and manage through CI/CD processes.