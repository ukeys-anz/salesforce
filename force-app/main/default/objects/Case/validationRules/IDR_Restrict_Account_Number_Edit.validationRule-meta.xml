<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IDR_Restrict_Account_Number_Edit</fullName>
    <active>true</active>
    <description>Restrict editing the account number once entered to support the collections process</description>
    <errorConditionFormula>AND(
$Setup.Salesforce_Library_Settings__c.Disable_VRs__c = false,
OR(AND (PRIORVALUE(IDR_Second_Issue__c) == true ,  ISCHANGED(IDR_Account_Card_Policy_Number_2__c) ), 
AND (PRIORVALUE(IDR_Third_Issue__c) == true ,  ISCHANGED(IDR_Account_Card_Policy_Number_3__c) ),
ISCHANGED(IDR_Account_Card_Policy_Number__c)
),NOT(ISCHANGED(RecordTypeId)))</errorConditionFormula>
    <errorMessage>You are not authorised to edit the account numbers.</errorMessage>
</ValidationRule>
