<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CCRM_Owner_Change</fullName>
    <active>true</active>
    <description>Only owner is allowed to make update on the task</description>
    <errorConditionFormula>AND(
    $Setup.Salesforce_Library_Settings__c.Disable_VRs__c = false,
    NOT(ISNEW()),
	OR(
		AND(	
            RecordType.DeveloperName = &apos;MLCRM_Task&apos;,
            $User.Profile_Name__c = &apos;ANZ CCRM Standard User&apos;
		),
		AND(
			RecordType.DeveloperName = &apos;CCRM_Task&apos;,
            $User.Profile_Name__c = &apos;ANZ ML Standard User&apos;
		)
	),
	$User.Id &lt;&gt; OwnerId
)</errorConditionFormula>
    <errorMessage>Only the task owner are allowed to edit this task.</errorMessage>
</ValidationRule>
