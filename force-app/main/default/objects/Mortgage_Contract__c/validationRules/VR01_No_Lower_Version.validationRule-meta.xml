<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR01_No_Lower_Version</fullName>
    <active>true</active>
    <description>Ensure incoming version is not lower than version currently in Salesforce</description>
    <errorConditionFormula>AND(NOT(ISNEW()),PRIORVALUE(Version__c) &gt; Version__c)</errorConditionFormula>
    <errorMessage>The version cannot be older than the current value</errorMessage>
</ValidationRule>
