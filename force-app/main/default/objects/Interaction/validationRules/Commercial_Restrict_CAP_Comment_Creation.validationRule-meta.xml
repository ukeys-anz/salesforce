<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Commercial_Restrict_CAP_Comment_Creation</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    $Setup.Salesforce_Library_Settings__c.Disable_VRs__c = false,
    $Profile.Name = &apos;ANZ CCRM Standard User&apos;,
    RecordType.DeveloperName = &apos;CCRM_Interaction&apos;,
    ISPICKVAL(Category__c, &apos;CAP Diary Comments&apos;),
    OR(
        NOT(ISBLANK(COI_Member__c)),
        NOT(ISBLANK(Broker__c)),
        NOT(ISBLANK(Opportunity__c))
    )
)</errorConditionFormula>
    <errorMessage>You cannot create or update CAP Diary Comments from Opportunities, Circle of Influence records, or for Accredited Brokers.</errorMessage>
</ValidationRule>
