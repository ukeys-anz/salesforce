<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Link_Appointment_to_both_Account_N_Lead</fullName>
    <active>true</active>
    <description>Do not allow user to link a single appointment to both Account and Lead at the same time</description>
    <errorConditionFormula>AND(RecordType.DeveloperName = &quot;Appointment&quot;, NOT(ISBLANK( AccountId )), NOT(ISBLANK( Lead__c )))</errorConditionFormula>
    <errorMessage>Please make sure that interaction is linked to an account or a lead and not both</errorMessage>
</ValidationRule>
