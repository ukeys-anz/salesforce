<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Expiry_Date__c</fullName>
    <businessStatus>Active</businessStatus>
    <complianceGroup>Privacy_Non_Personal_Information;Source_Salesforce;Integrity_Accurate</complianceGroup>
    <defaultValue>IF(NOT(CONTAINS( $User.Username,&apos;unica.integration@anzx.com&apos;)),
CASE( $RecordType.Name ,&apos;Commercial CRM Lead&apos;, IF((WEEKDAY(DATEVALUE(NOW() + (VALUE($Label.CCRM_ExpiryDateDays) - 1))) = 0), NOW() + (VALUE($Label.CCRM_ExpiryDateDays) + 1),IF((WEEKDAY(DATEVALUE(NOW() + (VALUE($Label.CCRM_ExpiryDateDays) - 1))) = 1), NOW() + VALUE($Label.CCRM_ExpiryDateDays) , NOW() + (VALUE($Label.CCRM_ExpiryDateDays) - 1))),NULL
),NULL)</defaultValue>
    <description>Expiry date of the lead.</description>
    <externalId>false</externalId>
    <label>Expiry Date</label>
    <required>false</required>
    <securityClassification>Internal</securityClassification>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
