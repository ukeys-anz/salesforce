<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Expiry_Date__c</fullName>
    <businessStatus>Active</businessStatus>
    <complianceGroup>Integrity - Accurate;Privacy - Non Personal Information;Source - Salesforce</complianceGroup>
    <defaultValue>IF(
    NOT(
        CONTAINS(
            $User.Username,&apos;unica.integration@anzx.com&apos;
            )
    ),
    CASE(
        $RecordType.Name ,&apos;Commercial CRM Lead&apos;,
            IF((WEEKDAY(ADDMONTHS(TODAY(), 6)) = 1)
                , ADDMONTHS(TODAY()+ 1, 6),
                    IF((WEEKDAY(ADDMONTHS(TODAY(), 6)) = 7)
                        , ADDMONTHS(TODAY()+ 2, 6)
                        , ADDMONTHS(TODAY(), 6)
                    )
            ),
            &apos;Referral&apos;,
            IF((WEEKDAY(ADDMONTHS(TODAY(), 6)) = 1)
                , ADDMONTHS(TODAY()+ 1, 6),
                IF((WEEKDAY(ADDMONTHS(TODAY(), 6)) = 7)
                    , ADDMONTHS(TODAY()+ 2, 6)
                    , ADDMONTHS(TODAY(), 6)
                )
            ),
            NULL
    )
    ,NULL
)</defaultValue>
    <description>Expiry date of the lead.</description>
    <externalId>false</externalId>
    <label>Expiry Date</label>
    <required>false</required>
    <securityClassification>Internal</securityClassification>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>true</trackHistory>
    <type>Date</type>
</CustomField>
