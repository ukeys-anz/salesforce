<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LeadPriority_Check</fullName>
    <active>true</active>
    <description>Validation to check priority field is read only for unica lead</description>
    <errorConditionFormula>AND(
  $Setup.Salesforce_Library_Settings__c.Disable_VRs__c = false,
  RecordType.DeveloperName = &apos;MLCRM_Lead&apos;,
  !ISNEW(),
  ISCHANGED(Priority__c),
  NOT(ISCHANGED(Skip_Validations__c)),
  NOT($Profile.Name = &quot;System Administrator&quot;),
  Text(Lead_Originator__c) = &apos;unica&apos;
)</errorConditionFormula>
    <errorDisplayField>Priority__c</errorDisplayField>
    <errorMessage>Lead priority cannot be changed for unica lead</errorMessage>
</ValidationRule>
