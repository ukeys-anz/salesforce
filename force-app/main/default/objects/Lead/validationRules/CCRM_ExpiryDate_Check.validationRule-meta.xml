<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CCRM_ExpiryDate_Check</fullName>
    <active>true</active>
    <description>Expiry Date should not be a past date</description>
    <errorConditionFormula>AND(
        $Setup.Salesforce_Library_Settings__c.Disable_VRs__c = false,
        RecordType.DeveloperName = &apos;CCRM_Lead&apos;,
        Expiry_Date__c &lt;  TODAY(),
        (ISCHANGED( Expiry_Date__c ) || ISNEW())
    )</errorConditionFormula>
    <errorDisplayField>Expiry_Date__c</errorDisplayField>
    <errorMessage>Expiry Date cannot be a past date</errorMessage>
</ValidationRule>
